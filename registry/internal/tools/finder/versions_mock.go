package finder

// Code generated by http://github.com/gojuno/minimock (dev). DO NOT EDIT.

import (
	"sync"
	mm_atomic "sync/atomic"
	mm_time "time"

	"github.com/gojuno/minimock/v3"
)

// VersionsMock implements Versions
type VersionsMock struct {
	t minimock.Tester

	funcRequest          func(source string) (rp1 *Result, err error)
	inspectFuncRequest   func(source string)
	afterRequestCounter  uint64
	beforeRequestCounter uint64
	RequestMock          mVersionsMockRequest
}

// NewVersionsMock returns a mock for Versions
func NewVersionsMock(t minimock.Tester) *VersionsMock {
	m := &VersionsMock{t: t}
	if controller, ok := t.(minimock.MockController); ok {
		controller.RegisterMocker(m)
	}

	m.RequestMock = mVersionsMockRequest{mock: m}
	m.RequestMock.callArgs = []*VersionsMockRequestParams{}

	return m
}

type mVersionsMockRequest struct {
	mock               *VersionsMock
	defaultExpectation *VersionsMockRequestExpectation
	expectations       []*VersionsMockRequestExpectation

	callArgs []*VersionsMockRequestParams
	mutex    sync.RWMutex
}

// VersionsMockRequestExpectation specifies expectation struct of the Versions.Request
type VersionsMockRequestExpectation struct {
	mock    *VersionsMock
	params  *VersionsMockRequestParams
	results *VersionsMockRequestResults
	Counter uint64
}

// VersionsMockRequestParams contains parameters of the Versions.Request
type VersionsMockRequestParams struct {
	source string
}

// VersionsMockRequestResults contains results of the Versions.Request
type VersionsMockRequestResults struct {
	rp1 *Result
	err error
}

// Expect sets up expected params for Versions.Request
func (mmRequest *mVersionsMockRequest) Expect(source string) *mVersionsMockRequest {
	if mmRequest.mock.funcRequest != nil {
		mmRequest.mock.t.Fatalf("VersionsMock.Request mock is already set by Set")
	}

	if mmRequest.defaultExpectation == nil {
		mmRequest.defaultExpectation = &VersionsMockRequestExpectation{}
	}

	mmRequest.defaultExpectation.params = &VersionsMockRequestParams{source}
	for _, e := range mmRequest.expectations {
		if minimock.Equal(e.params, mmRequest.defaultExpectation.params) {
			mmRequest.mock.t.Fatalf("Expectation set by When has same params: %#v", *mmRequest.defaultExpectation.params)
		}
	}

	return mmRequest
}

// Inspect accepts an inspector function that has same arguments as the Versions.Request
func (mmRequest *mVersionsMockRequest) Inspect(f func(source string)) *mVersionsMockRequest {
	if mmRequest.mock.inspectFuncRequest != nil {
		mmRequest.mock.t.Fatalf("Inspect function is already set for VersionsMock.Request")
	}

	mmRequest.mock.inspectFuncRequest = f

	return mmRequest
}

// Return sets up results that will be returned by Versions.Request
func (mmRequest *mVersionsMockRequest) Return(rp1 *Result, err error) *VersionsMock {
	if mmRequest.mock.funcRequest != nil {
		mmRequest.mock.t.Fatalf("VersionsMock.Request mock is already set by Set")
	}

	if mmRequest.defaultExpectation == nil {
		mmRequest.defaultExpectation = &VersionsMockRequestExpectation{mock: mmRequest.mock}
	}
	mmRequest.defaultExpectation.results = &VersionsMockRequestResults{rp1, err}
	return mmRequest.mock
}

//Set uses given function f to mock the Versions.Request method
func (mmRequest *mVersionsMockRequest) Set(f func(source string) (rp1 *Result, err error)) *VersionsMock {
	if mmRequest.defaultExpectation != nil {
		mmRequest.mock.t.Fatalf("Default expectation is already set for the Versions.Request method")
	}

	if len(mmRequest.expectations) > 0 {
		mmRequest.mock.t.Fatalf("Some expectations are already set for the Versions.Request method")
	}

	mmRequest.mock.funcRequest = f
	return mmRequest.mock
}

// When sets expectation for the Versions.Request which will trigger the result defined by the following
// Then helper
func (mmRequest *mVersionsMockRequest) When(source string) *VersionsMockRequestExpectation {
	if mmRequest.mock.funcRequest != nil {
		mmRequest.mock.t.Fatalf("VersionsMock.Request mock is already set by Set")
	}

	expectation := &VersionsMockRequestExpectation{
		mock:   mmRequest.mock,
		params: &VersionsMockRequestParams{source},
	}
	mmRequest.expectations = append(mmRequest.expectations, expectation)
	return expectation
}

// Then sets up Versions.Request return parameters for the expectation previously defined by the When method
func (e *VersionsMockRequestExpectation) Then(rp1 *Result, err error) *VersionsMock {
	e.results = &VersionsMockRequestResults{rp1, err}
	return e.mock
}

// Request implements Versions
func (mmRequest *VersionsMock) Request(source string) (rp1 *Result, err error) {
	mm_atomic.AddUint64(&mmRequest.beforeRequestCounter, 1)
	defer mm_atomic.AddUint64(&mmRequest.afterRequestCounter, 1)

	if mmRequest.inspectFuncRequest != nil {
		mmRequest.inspectFuncRequest(source)
	}

	mm_params := &VersionsMockRequestParams{source}

	// Record call args
	mmRequest.RequestMock.mutex.Lock()
	mmRequest.RequestMock.callArgs = append(mmRequest.RequestMock.callArgs, mm_params)
	mmRequest.RequestMock.mutex.Unlock()

	for _, e := range mmRequest.RequestMock.expectations {
		if minimock.Equal(e.params, mm_params) {
			mm_atomic.AddUint64(&e.Counter, 1)
			return e.results.rp1, e.results.err
		}
	}

	if mmRequest.RequestMock.defaultExpectation != nil {
		mm_atomic.AddUint64(&mmRequest.RequestMock.defaultExpectation.Counter, 1)
		mm_want := mmRequest.RequestMock.defaultExpectation.params
		mm_got := VersionsMockRequestParams{source}
		if mm_want != nil && !minimock.Equal(*mm_want, mm_got) {
			mmRequest.t.Errorf("VersionsMock.Request got unexpected parameters, want: %#v, got: %#v%s\n", *mm_want, mm_got, minimock.Diff(*mm_want, mm_got))
		}

		mm_results := mmRequest.RequestMock.defaultExpectation.results
		if mm_results == nil {
			mmRequest.t.Fatal("No results are set for the VersionsMock.Request")
		}
		return (*mm_results).rp1, (*mm_results).err
	}
	if mmRequest.funcRequest != nil {
		return mmRequest.funcRequest(source)
	}
	mmRequest.t.Fatalf("Unexpected call to VersionsMock.Request. %v", source)
	return
}

// RequestAfterCounter returns a count of finished VersionsMock.Request invocations
func (mmRequest *VersionsMock) RequestAfterCounter() uint64 {
	return mm_atomic.LoadUint64(&mmRequest.afterRequestCounter)
}

// RequestBeforeCounter returns a count of VersionsMock.Request invocations
func (mmRequest *VersionsMock) RequestBeforeCounter() uint64 {
	return mm_atomic.LoadUint64(&mmRequest.beforeRequestCounter)
}

// Calls returns a list of arguments used in each call to VersionsMock.Request.
// The list is in the same order as the calls were made (i.e. recent calls have a higher index)
func (mmRequest *mVersionsMockRequest) Calls() []*VersionsMockRequestParams {
	mmRequest.mutex.RLock()

	argCopy := make([]*VersionsMockRequestParams, len(mmRequest.callArgs))
	copy(argCopy, mmRequest.callArgs)

	mmRequest.mutex.RUnlock()

	return argCopy
}

// MinimockRequestDone returns true if the count of the Request invocations corresponds
// the number of defined expectations
func (m *VersionsMock) MinimockRequestDone() bool {
	for _, e := range m.RequestMock.expectations {
		if mm_atomic.LoadUint64(&e.Counter) < 1 {
			return false
		}
	}

	// if default expectation was set then invocations count should be greater than zero
	if m.RequestMock.defaultExpectation != nil && mm_atomic.LoadUint64(&m.afterRequestCounter) < 1 {
		return false
	}
	// if func was set then invocations count should be greater than zero
	if m.funcRequest != nil && mm_atomic.LoadUint64(&m.afterRequestCounter) < 1 {
		return false
	}
	return true
}

// MinimockRequestInspect logs each unmet expectation
func (m *VersionsMock) MinimockRequestInspect() {
	for _, e := range m.RequestMock.expectations {
		if mm_atomic.LoadUint64(&e.Counter) < 1 {
			m.t.Errorf("Expected call to VersionsMock.Request with params: %#v", *e.params)
		}
	}

	// if default expectation was set then invocations count should be greater than zero
	if m.RequestMock.defaultExpectation != nil && mm_atomic.LoadUint64(&m.afterRequestCounter) < 1 {
		if m.RequestMock.defaultExpectation.params == nil {
			m.t.Error("Expected call to VersionsMock.Request")
		} else {
			m.t.Errorf("Expected call to VersionsMock.Request with params: %#v", *m.RequestMock.defaultExpectation.params)
		}
	}
	// if func was set then invocations count should be greater than zero
	if m.funcRequest != nil && mm_atomic.LoadUint64(&m.afterRequestCounter) < 1 {
		m.t.Error("Expected call to VersionsMock.Request")
	}
}

// MinimockFinish checks that all mocked methods have been called the expected number of times
func (m *VersionsMock) MinimockFinish() {
	if !m.minimockDone() {
		m.MinimockRequestInspect()
		m.t.FailNow()
	}
}

// MinimockWait waits for all mocked methods to be called the expected number of times
func (m *VersionsMock) MinimockWait(timeout mm_time.Duration) {
	timeoutCh := mm_time.After(timeout)
	for {
		if m.minimockDone() {
			return
		}
		select {
		case <-timeoutCh:
			m.MinimockFinish()
			return
		case <-mm_time.After(10 * mm_time.Millisecond):
		}
	}
}

func (m *VersionsMock) minimockDone() bool {
	done := true
	return done &&
		m.MinimockRequestDone()
}
