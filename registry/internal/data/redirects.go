package data

import "github.com/modprox/libmodprox/repository"

func (s *store) ListRedirects() ([]repository.Redirect, error) {
	return nil, nil
}

func (s *store) AddRedirect(redirect repository.Redirect) error {
	return nil
}
