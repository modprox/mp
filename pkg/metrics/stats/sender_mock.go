package stats

// Code generated by http://github.com/gojuno/minimock (dev). DO NOT EDIT.

import (
	"sync"
	mm_atomic "sync/atomic"
	"time"
	mm_time "time"

	"github.com/gojuno/minimock/v3"
)

// SenderMock implements Sender
type SenderMock struct {
	t minimock.Tester

	funcCount          func(metric string, i int)
	inspectFuncCount   func(metric string, i int)
	afterCountCounter  uint64
	beforeCountCounter uint64
	CountMock          mSenderMockCount

	funcGauge          func(metric string, n int)
	inspectFuncGauge   func(metric string, n int)
	afterGaugeCounter  uint64
	beforeGaugeCounter uint64
	GaugeMock          mSenderMockGauge

	funcGaugeMS          func(metric string, t time.Time)
	inspectFuncGaugeMS   func(metric string, t time.Time)
	afterGaugeMSCounter  uint64
	beforeGaugeMSCounter uint64
	GaugeMSMock          mSenderMockGaugeMS
}

// NewSenderMock returns a mock for Sender
func NewSenderMock(t minimock.Tester) *SenderMock {
	m := &SenderMock{t: t}
	if controller, ok := t.(minimock.MockController); ok {
		controller.RegisterMocker(m)
	}

	m.CountMock = mSenderMockCount{mock: m}
	m.CountMock.callArgs = []*SenderMockCountParams{}

	m.GaugeMock = mSenderMockGauge{mock: m}
	m.GaugeMock.callArgs = []*SenderMockGaugeParams{}

	m.GaugeMSMock = mSenderMockGaugeMS{mock: m}
	m.GaugeMSMock.callArgs = []*SenderMockGaugeMSParams{}

	return m
}

type mSenderMockCount struct {
	mock               *SenderMock
	defaultExpectation *SenderMockCountExpectation
	expectations       []*SenderMockCountExpectation

	callArgs []*SenderMockCountParams
	mutex    sync.RWMutex
}

// SenderMockCountExpectation specifies expectation struct of the Sender.Count
type SenderMockCountExpectation struct {
	mock   *SenderMock
	params *SenderMockCountParams

	Counter uint64
}

// SenderMockCountParams contains parameters of the Sender.Count
type SenderMockCountParams struct {
	metric string
	i      int
}

// Expect sets up expected params for Sender.Count
func (mmCount *mSenderMockCount) Expect(metric string, i int) *mSenderMockCount {
	if mmCount.mock.funcCount != nil {
		mmCount.mock.t.Fatalf("SenderMock.Count mock is already set by Set")
	}

	if mmCount.defaultExpectation == nil {
		mmCount.defaultExpectation = &SenderMockCountExpectation{}
	}

	mmCount.defaultExpectation.params = &SenderMockCountParams{metric, i}
	for _, e := range mmCount.expectations {
		if minimock.Equal(e.params, mmCount.defaultExpectation.params) {
			mmCount.mock.t.Fatalf("Expectation set by When has same params: %#v", *mmCount.defaultExpectation.params)
		}
	}

	return mmCount
}

// Inspect accepts an inspector function that has same arguments as the Sender.Count
func (mmCount *mSenderMockCount) Inspect(f func(metric string, i int)) *mSenderMockCount {
	if mmCount.mock.inspectFuncCount != nil {
		mmCount.mock.t.Fatalf("Inspect function is already set for SenderMock.Count")
	}

	mmCount.mock.inspectFuncCount = f

	return mmCount
}

// Return sets up results that will be returned by Sender.Count
func (mmCount *mSenderMockCount) Return() *SenderMock {
	if mmCount.mock.funcCount != nil {
		mmCount.mock.t.Fatalf("SenderMock.Count mock is already set by Set")
	}

	if mmCount.defaultExpectation == nil {
		mmCount.defaultExpectation = &SenderMockCountExpectation{mock: mmCount.mock}
	}

	return mmCount.mock
}

//Set uses given function f to mock the Sender.Count method
func (mmCount *mSenderMockCount) Set(f func(metric string, i int)) *SenderMock {
	if mmCount.defaultExpectation != nil {
		mmCount.mock.t.Fatalf("Default expectation is already set for the Sender.Count method")
	}

	if len(mmCount.expectations) > 0 {
		mmCount.mock.t.Fatalf("Some expectations are already set for the Sender.Count method")
	}

	mmCount.mock.funcCount = f
	return mmCount.mock
}

// Count implements Sender
func (mmCount *SenderMock) Count(metric string, i int) {
	mm_atomic.AddUint64(&mmCount.beforeCountCounter, 1)
	defer mm_atomic.AddUint64(&mmCount.afterCountCounter, 1)

	if mmCount.inspectFuncCount != nil {
		mmCount.inspectFuncCount(metric, i)
	}

	mm_params := &SenderMockCountParams{metric, i}

	// Record call args
	mmCount.CountMock.mutex.Lock()
	mmCount.CountMock.callArgs = append(mmCount.CountMock.callArgs, mm_params)
	mmCount.CountMock.mutex.Unlock()

	for _, e := range mmCount.CountMock.expectations {
		if minimock.Equal(e.params, mm_params) {
			mm_atomic.AddUint64(&e.Counter, 1)
			return
		}
	}

	if mmCount.CountMock.defaultExpectation != nil {
		mm_atomic.AddUint64(&mmCount.CountMock.defaultExpectation.Counter, 1)
		mm_want := mmCount.CountMock.defaultExpectation.params
		mm_got := SenderMockCountParams{metric, i}
		if mm_want != nil && !minimock.Equal(*mm_want, mm_got) {
			mmCount.t.Errorf("SenderMock.Count got unexpected parameters, want: %#v, got: %#v%s\n", *mm_want, mm_got, minimock.Diff(*mm_want, mm_got))
		}

		return

	}
	if mmCount.funcCount != nil {
		mmCount.funcCount(metric, i)
		return
	}
	mmCount.t.Fatalf("Unexpected call to SenderMock.Count. %v %v", metric, i)

}

// CountAfterCounter returns a count of finished SenderMock.Count invocations
func (mmCount *SenderMock) CountAfterCounter() uint64 {
	return mm_atomic.LoadUint64(&mmCount.afterCountCounter)
}

// CountBeforeCounter returns a count of SenderMock.Count invocations
func (mmCount *SenderMock) CountBeforeCounter() uint64 {
	return mm_atomic.LoadUint64(&mmCount.beforeCountCounter)
}

// Calls returns a list of arguments used in each call to SenderMock.Count.
// The list is in the same order as the calls were made (i.e. recent calls have a higher index)
func (mmCount *mSenderMockCount) Calls() []*SenderMockCountParams {
	mmCount.mutex.RLock()

	argCopy := make([]*SenderMockCountParams, len(mmCount.callArgs))
	copy(argCopy, mmCount.callArgs)

	mmCount.mutex.RUnlock()

	return argCopy
}

// MinimockCountDone returns true if the count of the Count invocations corresponds
// the number of defined expectations
func (m *SenderMock) MinimockCountDone() bool {
	for _, e := range m.CountMock.expectations {
		if mm_atomic.LoadUint64(&e.Counter) < 1 {
			return false
		}
	}

	// if default expectation was set then invocations count should be greater than zero
	if m.CountMock.defaultExpectation != nil && mm_atomic.LoadUint64(&m.afterCountCounter) < 1 {
		return false
	}
	// if func was set then invocations count should be greater than zero
	if m.funcCount != nil && mm_atomic.LoadUint64(&m.afterCountCounter) < 1 {
		return false
	}
	return true
}

// MinimockCountInspect logs each unmet expectation
func (m *SenderMock) MinimockCountInspect() {
	for _, e := range m.CountMock.expectations {
		if mm_atomic.LoadUint64(&e.Counter) < 1 {
			m.t.Errorf("Expected call to SenderMock.Count with params: %#v", *e.params)
		}
	}

	// if default expectation was set then invocations count should be greater than zero
	if m.CountMock.defaultExpectation != nil && mm_atomic.LoadUint64(&m.afterCountCounter) < 1 {
		if m.CountMock.defaultExpectation.params == nil {
			m.t.Error("Expected call to SenderMock.Count")
		} else {
			m.t.Errorf("Expected call to SenderMock.Count with params: %#v", *m.CountMock.defaultExpectation.params)
		}
	}
	// if func was set then invocations count should be greater than zero
	if m.funcCount != nil && mm_atomic.LoadUint64(&m.afterCountCounter) < 1 {
		m.t.Error("Expected call to SenderMock.Count")
	}
}

type mSenderMockGauge struct {
	mock               *SenderMock
	defaultExpectation *SenderMockGaugeExpectation
	expectations       []*SenderMockGaugeExpectation

	callArgs []*SenderMockGaugeParams
	mutex    sync.RWMutex
}

// SenderMockGaugeExpectation specifies expectation struct of the Sender.Gauge
type SenderMockGaugeExpectation struct {
	mock   *SenderMock
	params *SenderMockGaugeParams

	Counter uint64
}

// SenderMockGaugeParams contains parameters of the Sender.Gauge
type SenderMockGaugeParams struct {
	metric string
	n      int
}

// Expect sets up expected params for Sender.Gauge
func (mmGauge *mSenderMockGauge) Expect(metric string, n int) *mSenderMockGauge {
	if mmGauge.mock.funcGauge != nil {
		mmGauge.mock.t.Fatalf("SenderMock.Gauge mock is already set by Set")
	}

	if mmGauge.defaultExpectation == nil {
		mmGauge.defaultExpectation = &SenderMockGaugeExpectation{}
	}

	mmGauge.defaultExpectation.params = &SenderMockGaugeParams{metric, n}
	for _, e := range mmGauge.expectations {
		if minimock.Equal(e.params, mmGauge.defaultExpectation.params) {
			mmGauge.mock.t.Fatalf("Expectation set by When has same params: %#v", *mmGauge.defaultExpectation.params)
		}
	}

	return mmGauge
}

// Inspect accepts an inspector function that has same arguments as the Sender.Gauge
func (mmGauge *mSenderMockGauge) Inspect(f func(metric string, n int)) *mSenderMockGauge {
	if mmGauge.mock.inspectFuncGauge != nil {
		mmGauge.mock.t.Fatalf("Inspect function is already set for SenderMock.Gauge")
	}

	mmGauge.mock.inspectFuncGauge = f

	return mmGauge
}

// Return sets up results that will be returned by Sender.Gauge
func (mmGauge *mSenderMockGauge) Return() *SenderMock {
	if mmGauge.mock.funcGauge != nil {
		mmGauge.mock.t.Fatalf("SenderMock.Gauge mock is already set by Set")
	}

	if mmGauge.defaultExpectation == nil {
		mmGauge.defaultExpectation = &SenderMockGaugeExpectation{mock: mmGauge.mock}
	}

	return mmGauge.mock
}

//Set uses given function f to mock the Sender.Gauge method
func (mmGauge *mSenderMockGauge) Set(f func(metric string, n int)) *SenderMock {
	if mmGauge.defaultExpectation != nil {
		mmGauge.mock.t.Fatalf("Default expectation is already set for the Sender.Gauge method")
	}

	if len(mmGauge.expectations) > 0 {
		mmGauge.mock.t.Fatalf("Some expectations are already set for the Sender.Gauge method")
	}

	mmGauge.mock.funcGauge = f
	return mmGauge.mock
}

// Gauge implements Sender
func (mmGauge *SenderMock) Gauge(metric string, n int) {
	mm_atomic.AddUint64(&mmGauge.beforeGaugeCounter, 1)
	defer mm_atomic.AddUint64(&mmGauge.afterGaugeCounter, 1)

	if mmGauge.inspectFuncGauge != nil {
		mmGauge.inspectFuncGauge(metric, n)
	}

	mm_params := &SenderMockGaugeParams{metric, n}

	// Record call args
	mmGauge.GaugeMock.mutex.Lock()
	mmGauge.GaugeMock.callArgs = append(mmGauge.GaugeMock.callArgs, mm_params)
	mmGauge.GaugeMock.mutex.Unlock()

	for _, e := range mmGauge.GaugeMock.expectations {
		if minimock.Equal(e.params, mm_params) {
			mm_atomic.AddUint64(&e.Counter, 1)
			return
		}
	}

	if mmGauge.GaugeMock.defaultExpectation != nil {
		mm_atomic.AddUint64(&mmGauge.GaugeMock.defaultExpectation.Counter, 1)
		mm_want := mmGauge.GaugeMock.defaultExpectation.params
		mm_got := SenderMockGaugeParams{metric, n}
		if mm_want != nil && !minimock.Equal(*mm_want, mm_got) {
			mmGauge.t.Errorf("SenderMock.Gauge got unexpected parameters, want: %#v, got: %#v%s\n", *mm_want, mm_got, minimock.Diff(*mm_want, mm_got))
		}

		return

	}
	if mmGauge.funcGauge != nil {
		mmGauge.funcGauge(metric, n)
		return
	}
	mmGauge.t.Fatalf("Unexpected call to SenderMock.Gauge. %v %v", metric, n)

}

// GaugeAfterCounter returns a count of finished SenderMock.Gauge invocations
func (mmGauge *SenderMock) GaugeAfterCounter() uint64 {
	return mm_atomic.LoadUint64(&mmGauge.afterGaugeCounter)
}

// GaugeBeforeCounter returns a count of SenderMock.Gauge invocations
func (mmGauge *SenderMock) GaugeBeforeCounter() uint64 {
	return mm_atomic.LoadUint64(&mmGauge.beforeGaugeCounter)
}

// Calls returns a list of arguments used in each call to SenderMock.Gauge.
// The list is in the same order as the calls were made (i.e. recent calls have a higher index)
func (mmGauge *mSenderMockGauge) Calls() []*SenderMockGaugeParams {
	mmGauge.mutex.RLock()

	argCopy := make([]*SenderMockGaugeParams, len(mmGauge.callArgs))
	copy(argCopy, mmGauge.callArgs)

	mmGauge.mutex.RUnlock()

	return argCopy
}

// MinimockGaugeDone returns true if the count of the Gauge invocations corresponds
// the number of defined expectations
func (m *SenderMock) MinimockGaugeDone() bool {
	for _, e := range m.GaugeMock.expectations {
		if mm_atomic.LoadUint64(&e.Counter) < 1 {
			return false
		}
	}

	// if default expectation was set then invocations count should be greater than zero
	if m.GaugeMock.defaultExpectation != nil && mm_atomic.LoadUint64(&m.afterGaugeCounter) < 1 {
		return false
	}
	// if func was set then invocations count should be greater than zero
	if m.funcGauge != nil && mm_atomic.LoadUint64(&m.afterGaugeCounter) < 1 {
		return false
	}
	return true
}

// MinimockGaugeInspect logs each unmet expectation
func (m *SenderMock) MinimockGaugeInspect() {
	for _, e := range m.GaugeMock.expectations {
		if mm_atomic.LoadUint64(&e.Counter) < 1 {
			m.t.Errorf("Expected call to SenderMock.Gauge with params: %#v", *e.params)
		}
	}

	// if default expectation was set then invocations count should be greater than zero
	if m.GaugeMock.defaultExpectation != nil && mm_atomic.LoadUint64(&m.afterGaugeCounter) < 1 {
		if m.GaugeMock.defaultExpectation.params == nil {
			m.t.Error("Expected call to SenderMock.Gauge")
		} else {
			m.t.Errorf("Expected call to SenderMock.Gauge with params: %#v", *m.GaugeMock.defaultExpectation.params)
		}
	}
	// if func was set then invocations count should be greater than zero
	if m.funcGauge != nil && mm_atomic.LoadUint64(&m.afterGaugeCounter) < 1 {
		m.t.Error("Expected call to SenderMock.Gauge")
	}
}

type mSenderMockGaugeMS struct {
	mock               *SenderMock
	defaultExpectation *SenderMockGaugeMSExpectation
	expectations       []*SenderMockGaugeMSExpectation

	callArgs []*SenderMockGaugeMSParams
	mutex    sync.RWMutex
}

// SenderMockGaugeMSExpectation specifies expectation struct of the Sender.GaugeMS
type SenderMockGaugeMSExpectation struct {
	mock   *SenderMock
	params *SenderMockGaugeMSParams

	Counter uint64
}

// SenderMockGaugeMSParams contains parameters of the Sender.GaugeMS
type SenderMockGaugeMSParams struct {
	metric string
	t      time.Time
}

// Expect sets up expected params for Sender.GaugeMS
func (mmGaugeMS *mSenderMockGaugeMS) Expect(metric string, t time.Time) *mSenderMockGaugeMS {
	if mmGaugeMS.mock.funcGaugeMS != nil {
		mmGaugeMS.mock.t.Fatalf("SenderMock.GaugeMS mock is already set by Set")
	}

	if mmGaugeMS.defaultExpectation == nil {
		mmGaugeMS.defaultExpectation = &SenderMockGaugeMSExpectation{}
	}

	mmGaugeMS.defaultExpectation.params = &SenderMockGaugeMSParams{metric, t}
	for _, e := range mmGaugeMS.expectations {
		if minimock.Equal(e.params, mmGaugeMS.defaultExpectation.params) {
			mmGaugeMS.mock.t.Fatalf("Expectation set by When has same params: %#v", *mmGaugeMS.defaultExpectation.params)
		}
	}

	return mmGaugeMS
}

// Inspect accepts an inspector function that has same arguments as the Sender.GaugeMS
func (mmGaugeMS *mSenderMockGaugeMS) Inspect(f func(metric string, t time.Time)) *mSenderMockGaugeMS {
	if mmGaugeMS.mock.inspectFuncGaugeMS != nil {
		mmGaugeMS.mock.t.Fatalf("Inspect function is already set for SenderMock.GaugeMS")
	}

	mmGaugeMS.mock.inspectFuncGaugeMS = f

	return mmGaugeMS
}

// Return sets up results that will be returned by Sender.GaugeMS
func (mmGaugeMS *mSenderMockGaugeMS) Return() *SenderMock {
	if mmGaugeMS.mock.funcGaugeMS != nil {
		mmGaugeMS.mock.t.Fatalf("SenderMock.GaugeMS mock is already set by Set")
	}

	if mmGaugeMS.defaultExpectation == nil {
		mmGaugeMS.defaultExpectation = &SenderMockGaugeMSExpectation{mock: mmGaugeMS.mock}
	}

	return mmGaugeMS.mock
}

//Set uses given function f to mock the Sender.GaugeMS method
func (mmGaugeMS *mSenderMockGaugeMS) Set(f func(metric string, t time.Time)) *SenderMock {
	if mmGaugeMS.defaultExpectation != nil {
		mmGaugeMS.mock.t.Fatalf("Default expectation is already set for the Sender.GaugeMS method")
	}

	if len(mmGaugeMS.expectations) > 0 {
		mmGaugeMS.mock.t.Fatalf("Some expectations are already set for the Sender.GaugeMS method")
	}

	mmGaugeMS.mock.funcGaugeMS = f
	return mmGaugeMS.mock
}

// GaugeMS implements Sender
func (mmGaugeMS *SenderMock) GaugeMS(metric string, t time.Time) {
	mm_atomic.AddUint64(&mmGaugeMS.beforeGaugeMSCounter, 1)
	defer mm_atomic.AddUint64(&mmGaugeMS.afterGaugeMSCounter, 1)

	if mmGaugeMS.inspectFuncGaugeMS != nil {
		mmGaugeMS.inspectFuncGaugeMS(metric, t)
	}

	mm_params := &SenderMockGaugeMSParams{metric, t}

	// Record call args
	mmGaugeMS.GaugeMSMock.mutex.Lock()
	mmGaugeMS.GaugeMSMock.callArgs = append(mmGaugeMS.GaugeMSMock.callArgs, mm_params)
	mmGaugeMS.GaugeMSMock.mutex.Unlock()

	for _, e := range mmGaugeMS.GaugeMSMock.expectations {
		if minimock.Equal(e.params, mm_params) {
			mm_atomic.AddUint64(&e.Counter, 1)
			return
		}
	}

	if mmGaugeMS.GaugeMSMock.defaultExpectation != nil {
		mm_atomic.AddUint64(&mmGaugeMS.GaugeMSMock.defaultExpectation.Counter, 1)
		mm_want := mmGaugeMS.GaugeMSMock.defaultExpectation.params
		mm_got := SenderMockGaugeMSParams{metric, t}
		if mm_want != nil && !minimock.Equal(*mm_want, mm_got) {
			mmGaugeMS.t.Errorf("SenderMock.GaugeMS got unexpected parameters, want: %#v, got: %#v%s\n", *mm_want, mm_got, minimock.Diff(*mm_want, mm_got))
		}

		return

	}
	if mmGaugeMS.funcGaugeMS != nil {
		mmGaugeMS.funcGaugeMS(metric, t)
		return
	}
	mmGaugeMS.t.Fatalf("Unexpected call to SenderMock.GaugeMS. %v %v", metric, t)

}

// GaugeMSAfterCounter returns a count of finished SenderMock.GaugeMS invocations
func (mmGaugeMS *SenderMock) GaugeMSAfterCounter() uint64 {
	return mm_atomic.LoadUint64(&mmGaugeMS.afterGaugeMSCounter)
}

// GaugeMSBeforeCounter returns a count of SenderMock.GaugeMS invocations
func (mmGaugeMS *SenderMock) GaugeMSBeforeCounter() uint64 {
	return mm_atomic.LoadUint64(&mmGaugeMS.beforeGaugeMSCounter)
}

// Calls returns a list of arguments used in each call to SenderMock.GaugeMS.
// The list is in the same order as the calls were made (i.e. recent calls have a higher index)
func (mmGaugeMS *mSenderMockGaugeMS) Calls() []*SenderMockGaugeMSParams {
	mmGaugeMS.mutex.RLock()

	argCopy := make([]*SenderMockGaugeMSParams, len(mmGaugeMS.callArgs))
	copy(argCopy, mmGaugeMS.callArgs)

	mmGaugeMS.mutex.RUnlock()

	return argCopy
}

// MinimockGaugeMSDone returns true if the count of the GaugeMS invocations corresponds
// the number of defined expectations
func (m *SenderMock) MinimockGaugeMSDone() bool {
	for _, e := range m.GaugeMSMock.expectations {
		if mm_atomic.LoadUint64(&e.Counter) < 1 {
			return false
		}
	}

	// if default expectation was set then invocations count should be greater than zero
	if m.GaugeMSMock.defaultExpectation != nil && mm_atomic.LoadUint64(&m.afterGaugeMSCounter) < 1 {
		return false
	}
	// if func was set then invocations count should be greater than zero
	if m.funcGaugeMS != nil && mm_atomic.LoadUint64(&m.afterGaugeMSCounter) < 1 {
		return false
	}
	return true
}

// MinimockGaugeMSInspect logs each unmet expectation
func (m *SenderMock) MinimockGaugeMSInspect() {
	for _, e := range m.GaugeMSMock.expectations {
		if mm_atomic.LoadUint64(&e.Counter) < 1 {
			m.t.Errorf("Expected call to SenderMock.GaugeMS with params: %#v", *e.params)
		}
	}

	// if default expectation was set then invocations count should be greater than zero
	if m.GaugeMSMock.defaultExpectation != nil && mm_atomic.LoadUint64(&m.afterGaugeMSCounter) < 1 {
		if m.GaugeMSMock.defaultExpectation.params == nil {
			m.t.Error("Expected call to SenderMock.GaugeMS")
		} else {
			m.t.Errorf("Expected call to SenderMock.GaugeMS with params: %#v", *m.GaugeMSMock.defaultExpectation.params)
		}
	}
	// if func was set then invocations count should be greater than zero
	if m.funcGaugeMS != nil && mm_atomic.LoadUint64(&m.afterGaugeMSCounter) < 1 {
		m.t.Error("Expected call to SenderMock.GaugeMS")
	}
}

// MinimockFinish checks that all mocked methods have been called the expected number of times
func (m *SenderMock) MinimockFinish() {
	if !m.minimockDone() {
		m.MinimockCountInspect()

		m.MinimockGaugeInspect()

		m.MinimockGaugeMSInspect()
		m.t.FailNow()
	}
}

// MinimockWait waits for all mocked methods to be called the expected number of times
func (m *SenderMock) MinimockWait(timeout mm_time.Duration) {
	timeoutCh := mm_time.After(timeout)
	for {
		if m.minimockDone() {
			return
		}
		select {
		case <-timeoutCh:
			m.MinimockFinish()
			return
		case <-mm_time.After(10 * mm_time.Millisecond):
		}
	}
}

func (m *SenderMock) minimockDone() bool {
	done := true
	return done &&
		m.MinimockCountDone() &&
		m.MinimockGaugeDone() &&
		m.MinimockGaugeMSDone()
}
