package zips

// Code generated by http://github.com/gojuno/minimock (dev). DO NOT EDIT.

import (
	"sync"
	mm_atomic "sync/atomic"
	mm_time "time"

	"github.com/gojuno/minimock"
	"oss.indeed.com/go/modprox/pkg/repository"
	"oss.indeed.com/go/modprox/pkg/upstream"
)

// ClientMock implements Client
type ClientMock struct {
	t minimock.Tester

	funcGet          func(rp1 *upstream.Request) (b1 repository.Blob, err error)
	afterGetCounter  uint64
	beforeGetCounter uint64
	GetMock          mClientMockGet

	funcProtocols          func() (sa1 []string)
	afterProtocolsCounter  uint64
	beforeProtocolsCounter uint64
	ProtocolsMock          mClientMockProtocols
}

// NewClientMock returns a mock for Client
func NewClientMock(t minimock.Tester) *ClientMock {
	m := &ClientMock{t: t}
	if controller, ok := t.(minimock.MockController); ok {
		controller.RegisterMocker(m)
	}

	m.GetMock = mClientMockGet{mock: m}
	m.GetMock.callArgs = []*ClientMockGetParams{}

	m.ProtocolsMock = mClientMockProtocols{mock: m}

	return m
}

type mClientMockGet struct {
	mock               *ClientMock
	defaultExpectation *ClientMockGetExpectation
	expectations       []*ClientMockGetExpectation

	callArgs []*ClientMockGetParams
	mutex    sync.RWMutex
}

// ClientMockGetExpectation specifies expectation struct of the Client.Get
type ClientMockGetExpectation struct {
	mock    *ClientMock
	params  *ClientMockGetParams
	results *ClientMockGetResults
	Counter uint64
}

// ClientMockGetParams contains parameters of the Client.Get
type ClientMockGetParams struct {
	rp1 *upstream.Request
}

// ClientMockGetResults contains results of the Client.Get
type ClientMockGetResults struct {
	b1  repository.Blob
	err error
}

// Expect sets up expected params for Client.Get
func (mmGet *mClientMockGet) Expect(rp1 *upstream.Request) *mClientMockGet {
	if mmGet.mock.funcGet != nil {
		mmGet.mock.t.Fatalf("ClientMock.Get mock is already set by Set")
	}

	if mmGet.defaultExpectation == nil {
		mmGet.defaultExpectation = &ClientMockGetExpectation{}
	}

	mmGet.defaultExpectation.params = &ClientMockGetParams{rp1}
	for _, e := range mmGet.expectations {
		if minimock.Equal(e.params, mmGet.defaultExpectation.params) {
			mmGet.mock.t.Fatalf("Expectation set by When has same params: %#v", *mmGet.defaultExpectation.params)
		}
	}

	return mmGet
}

// Return sets up results that will be returned by Client.Get
func (mmGet *mClientMockGet) Return(b1 repository.Blob, err error) *ClientMock {
	if mmGet.mock.funcGet != nil {
		mmGet.mock.t.Fatalf("ClientMock.Get mock is already set by Set")
	}

	if mmGet.defaultExpectation == nil {
		mmGet.defaultExpectation = &ClientMockGetExpectation{mock: mmGet.mock}
	}
	mmGet.defaultExpectation.results = &ClientMockGetResults{b1, err}
	return mmGet.mock
}

//Set uses given function f to mock the Client.Get method
func (mmGet *mClientMockGet) Set(f func(rp1 *upstream.Request) (b1 repository.Blob, err error)) *ClientMock {
	if mmGet.defaultExpectation != nil {
		mmGet.mock.t.Fatalf("Default expectation is already set for the Client.Get method")
	}

	if len(mmGet.expectations) > 0 {
		mmGet.mock.t.Fatalf("Some expectations are already set for the Client.Get method")
	}

	mmGet.mock.funcGet = f
	return mmGet.mock
}

// When sets expectation for the Client.Get which will trigger the result defined by the following
// Then helper
func (mmGet *mClientMockGet) When(rp1 *upstream.Request) *ClientMockGetExpectation {
	if mmGet.mock.funcGet != nil {
		mmGet.mock.t.Fatalf("ClientMock.Get mock is already set by Set")
	}

	expectation := &ClientMockGetExpectation{
		mock:   mmGet.mock,
		params: &ClientMockGetParams{rp1},
	}
	mmGet.expectations = append(mmGet.expectations, expectation)
	return expectation
}

// Then sets up Client.Get return parameters for the expectation previously defined by the When method
func (e *ClientMockGetExpectation) Then(b1 repository.Blob, err error) *ClientMock {
	e.results = &ClientMockGetResults{b1, err}
	return e.mock
}

// Get implements Client
func (mmGet *ClientMock) Get(rp1 *upstream.Request) (b1 repository.Blob, err error) {
	mm_atomic.AddUint64(&mmGet.beforeGetCounter, 1)
	defer mm_atomic.AddUint64(&mmGet.afterGetCounter, 1)

	params := &ClientMockGetParams{rp1}

	// Record call args
	mmGet.GetMock.mutex.Lock()
	mmGet.GetMock.callArgs = append(mmGet.GetMock.callArgs, params)
	mmGet.GetMock.mutex.Unlock()

	for _, e := range mmGet.GetMock.expectations {
		if minimock.Equal(e.params, params) {
			mm_atomic.AddUint64(&e.Counter, 1)
			return e.results.b1, e.results.err
		}
	}

	if mmGet.GetMock.defaultExpectation != nil {
		mm_atomic.AddUint64(&mmGet.GetMock.defaultExpectation.Counter, 1)
		want := mmGet.GetMock.defaultExpectation.params
		got := ClientMockGetParams{rp1}
		if want != nil && !minimock.Equal(*want, got) {
			mmGet.t.Errorf("ClientMock.Get got unexpected parameters, want: %#v, got: %#v%s\n", *want, got, minimock.Diff(*want, got))
		}

		results := mmGet.GetMock.defaultExpectation.results
		if results == nil {
			mmGet.t.Fatal("No results are set for the ClientMock.Get")
		}
		return (*results).b1, (*results).err
	}
	if mmGet.funcGet != nil {
		return mmGet.funcGet(rp1)
	}
	mmGet.t.Fatalf("Unexpected call to ClientMock.Get. %v", rp1)
	return
}

// GetAfterCounter returns a count of finished ClientMock.Get invocations
func (mmGet *ClientMock) GetAfterCounter() uint64 {
	return mm_atomic.LoadUint64(&mmGet.afterGetCounter)
}

// GetBeforeCounter returns a count of ClientMock.Get invocations
func (mmGet *ClientMock) GetBeforeCounter() uint64 {
	return mm_atomic.LoadUint64(&mmGet.beforeGetCounter)
}

// Calls returns a list of arguments used in each call to ClientMock.Get.
// The list is in the same order as the calls were made (i.e. recent calls have a higher index)
func (mmGet *mClientMockGet) Calls() []*ClientMockGetParams {
	mmGet.mutex.RLock()

	argCopy := make([]*ClientMockGetParams, len(mmGet.callArgs))
	copy(argCopy, mmGet.callArgs)

	mmGet.mutex.RUnlock()

	return argCopy
}

// MinimockGetDone returns true if the count of the Get invocations corresponds
// the number of defined expectations
func (m *ClientMock) MinimockGetDone() bool {
	for _, e := range m.GetMock.expectations {
		if mm_atomic.LoadUint64(&e.Counter) < 1 {
			return false
		}
	}

	// if default expectation was set then invocations count should be greater than zero
	if m.GetMock.defaultExpectation != nil && mm_atomic.LoadUint64(&m.afterGetCounter) < 1 {
		return false
	}
	// if func was set then invocations count should be greater than zero
	if m.funcGet != nil && mm_atomic.LoadUint64(&m.afterGetCounter) < 1 {
		return false
	}
	return true
}

// MinimockGetInspect logs each unmet expectation
func (m *ClientMock) MinimockGetInspect() {
	for _, e := range m.GetMock.expectations {
		if mm_atomic.LoadUint64(&e.Counter) < 1 {
			m.t.Errorf("Expected call to ClientMock.Get with params: %#v", *e.params)
		}
	}

	// if default expectation was set then invocations count should be greater than zero
	if m.GetMock.defaultExpectation != nil && mm_atomic.LoadUint64(&m.afterGetCounter) < 1 {
		if m.GetMock.defaultExpectation.params == nil {
			m.t.Error("Expected call to ClientMock.Get")
		} else {
			m.t.Errorf("Expected call to ClientMock.Get with params: %#v", *m.GetMock.defaultExpectation.params)
		}
	}
	// if func was set then invocations count should be greater than zero
	if m.funcGet != nil && mm_atomic.LoadUint64(&m.afterGetCounter) < 1 {
		m.t.Error("Expected call to ClientMock.Get")
	}
}

type mClientMockProtocols struct {
	mock               *ClientMock
	defaultExpectation *ClientMockProtocolsExpectation
	expectations       []*ClientMockProtocolsExpectation
}

// ClientMockProtocolsExpectation specifies expectation struct of the Client.Protocols
type ClientMockProtocolsExpectation struct {
	mock *ClientMock

	results *ClientMockProtocolsResults
	Counter uint64
}

// ClientMockProtocolsResults contains results of the Client.Protocols
type ClientMockProtocolsResults struct {
	sa1 []string
}

// Expect sets up expected params for Client.Protocols
func (mmProtocols *mClientMockProtocols) Expect() *mClientMockProtocols {
	if mmProtocols.mock.funcProtocols != nil {
		mmProtocols.mock.t.Fatalf("ClientMock.Protocols mock is already set by Set")
	}

	if mmProtocols.defaultExpectation == nil {
		mmProtocols.defaultExpectation = &ClientMockProtocolsExpectation{}
	}

	return mmProtocols
}

// Return sets up results that will be returned by Client.Protocols
func (mmProtocols *mClientMockProtocols) Return(sa1 []string) *ClientMock {
	if mmProtocols.mock.funcProtocols != nil {
		mmProtocols.mock.t.Fatalf("ClientMock.Protocols mock is already set by Set")
	}

	if mmProtocols.defaultExpectation == nil {
		mmProtocols.defaultExpectation = &ClientMockProtocolsExpectation{mock: mmProtocols.mock}
	}
	mmProtocols.defaultExpectation.results = &ClientMockProtocolsResults{sa1}
	return mmProtocols.mock
}

//Set uses given function f to mock the Client.Protocols method
func (mmProtocols *mClientMockProtocols) Set(f func() (sa1 []string)) *ClientMock {
	if mmProtocols.defaultExpectation != nil {
		mmProtocols.mock.t.Fatalf("Default expectation is already set for the Client.Protocols method")
	}

	if len(mmProtocols.expectations) > 0 {
		mmProtocols.mock.t.Fatalf("Some expectations are already set for the Client.Protocols method")
	}

	mmProtocols.mock.funcProtocols = f
	return mmProtocols.mock
}

// Protocols implements Client
func (mmProtocols *ClientMock) Protocols() (sa1 []string) {
	mm_atomic.AddUint64(&mmProtocols.beforeProtocolsCounter, 1)
	defer mm_atomic.AddUint64(&mmProtocols.afterProtocolsCounter, 1)

	if mmProtocols.ProtocolsMock.defaultExpectation != nil {
		mm_atomic.AddUint64(&mmProtocols.ProtocolsMock.defaultExpectation.Counter, 1)

		results := mmProtocols.ProtocolsMock.defaultExpectation.results
		if results == nil {
			mmProtocols.t.Fatal("No results are set for the ClientMock.Protocols")
		}
		return (*results).sa1
	}
	if mmProtocols.funcProtocols != nil {
		return mmProtocols.funcProtocols()
	}
	mmProtocols.t.Fatalf("Unexpected call to ClientMock.Protocols.")
	return
}

// ProtocolsAfterCounter returns a count of finished ClientMock.Protocols invocations
func (mmProtocols *ClientMock) ProtocolsAfterCounter() uint64 {
	return mm_atomic.LoadUint64(&mmProtocols.afterProtocolsCounter)
}

// ProtocolsBeforeCounter returns a count of ClientMock.Protocols invocations
func (mmProtocols *ClientMock) ProtocolsBeforeCounter() uint64 {
	return mm_atomic.LoadUint64(&mmProtocols.beforeProtocolsCounter)
}

// MinimockProtocolsDone returns true if the count of the Protocols invocations corresponds
// the number of defined expectations
func (m *ClientMock) MinimockProtocolsDone() bool {
	for _, e := range m.ProtocolsMock.expectations {
		if mm_atomic.LoadUint64(&e.Counter) < 1 {
			return false
		}
	}

	// if default expectation was set then invocations count should be greater than zero
	if m.ProtocolsMock.defaultExpectation != nil && mm_atomic.LoadUint64(&m.afterProtocolsCounter) < 1 {
		return false
	}
	// if func was set then invocations count should be greater than zero
	if m.funcProtocols != nil && mm_atomic.LoadUint64(&m.afterProtocolsCounter) < 1 {
		return false
	}
	return true
}

// MinimockProtocolsInspect logs each unmet expectation
func (m *ClientMock) MinimockProtocolsInspect() {
	for _, e := range m.ProtocolsMock.expectations {
		if mm_atomic.LoadUint64(&e.Counter) < 1 {
			m.t.Error("Expected call to ClientMock.Protocols")
		}
	}

	// if default expectation was set then invocations count should be greater than zero
	if m.ProtocolsMock.defaultExpectation != nil && mm_atomic.LoadUint64(&m.afterProtocolsCounter) < 1 {
		m.t.Error("Expected call to ClientMock.Protocols")
	}
	// if func was set then invocations count should be greater than zero
	if m.funcProtocols != nil && mm_atomic.LoadUint64(&m.afterProtocolsCounter) < 1 {
		m.t.Error("Expected call to ClientMock.Protocols")
	}
}

// MinimockFinish checks that all mocked methods have been called the expected number of times
func (m *ClientMock) MinimockFinish() {
	if !m.minimockDone() {
		m.MinimockGetInspect()

		m.MinimockProtocolsInspect()
		m.t.FailNow()
	}
}

// MinimockWait waits for all mocked methods to be called the expected number of times
func (m *ClientMock) MinimockWait(timeout mm_time.Duration) {
	timeoutCh := mm_time.After(timeout)
	for {
		if m.minimockDone() {
			return
		}
		select {
		case <-timeoutCh:
			m.MinimockFinish()
			return
		case <-mm_time.After(10 * mm_time.Millisecond):
		}
	}
}

func (m *ClientMock) minimockDone() bool {
	done := true
	return done &&
		m.MinimockGetDone() &&
		m.MinimockProtocolsDone()
}
